/**
 * @file components/layout/index.ts
 * Barrel: generic application layout components (header, footer, etc.).
 * These are reusable across any page — not tied to the landing page.
 *
 *   import { AppHeader, AppFooter } from '@/app/components/layout'
 */
export { AppHeader } from "./AppHeader";
export { AppFooter } from "./AppFooter";
